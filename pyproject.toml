```toml
[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "tps-core"
version = "0.1.0"
description = "Ternary Pattern Search for HFT Analytics"
dependencies = [
    "numpy>=1.24.0",
    "pandas>=2.0.0",
    "matplotlib>=3.7.0"
]
